%p.kormapp-cartitem-name!= @product_title
%table
  %tbody
    %tr
      %td.kormapp-product-amount
        != @moneyWithoutCurrency @product_price
        x
        = @quantity
      %td.kormapp-separator
        .separator-placeholder
      // TODO Мультивалютность
      %td.kormapp-product-price= "#{@moneyWithoutCurrency @total_cost} р."